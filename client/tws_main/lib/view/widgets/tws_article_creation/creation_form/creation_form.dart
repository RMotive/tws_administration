part of '../tws_article_creation.dart';

class _CreationForm<TModel> extends StatelessWidget {
  final _TWSArticleCreationItemState<TModel> state;

  const _CreationForm(this.state);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
